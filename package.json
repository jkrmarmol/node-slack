{
  "dependencies": {
    "@slack/bolt": "^4.2.1",
    "typescript": "^5.8.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm i && tsc",
    "start": "node ./dist/server.js",
    "dev": "nodemon ./src/server.ts",
    "lint": "eslint src/"
  },
  "devDependencies": {
    "dotenv": "^16.4.7",
    "nodemon": "^3.1.9",
    "ts-node": "^10.9.2"
  }
}
